# Integration test findings — ServicePro

Generated by `tool/build_integration_report.dart` on 2026-09-06T13:46:14.742703.

Executed against the **live** Supabase project. This pass catalogues failures; it does not fix them.

## Summary

| Feature area | Passed | Failed | Skipped | Total |
|---|---:|---:|---:|---:|
| Work Orders / Permissions & RLS | 5 | 2 | 0 | 7 |
| **Total** | **5** | **2** | **0** | **7** |

## Manual cleanup required

These tables have neither a `deleted_at` column nor a `FOR DELETE` policy, so rows this run created are permanent. They are listed rather than silently dropped from the cleanup report.

| Table | Why it cannot be cleaned |
|---|---|
| `work_order_pause_requests` | No `deleted_at`, no `FOR DELETE` policy (finding F5). Two rows per lifecycle run. |
| `work_order_history` | Append-only immutable log. |
| `audit_logs` | Append-only. |
| `access_logs` | Append-only; every sign-in adds a row. |
| `sync_errors` | Append-only telemetry. |
| `service_provider_profiles` | No `deleted_at`; hard delete blocked by trigger (finding SCH-13). Deactivated instead. |

## Failures

### RLS-04 — has_permission() must deny a permission whose key is present but false
- Feature: Work Orders / Permissions & RLS  |  Role: technician (scoped)  |  Layer: behaviour
- Steps: grant the technician a group where every key is false -> attempt an insert gated by locations.create
- Expected: Denied. Predicted to FAIL (F1): has_permission ends in `v_permissions ? key`, and on a JSONB object `?` tests key existence, so "key": false grants access.
- Actual:   insert succeeded (1 row)
- Error:    TestFailure — "locations.create": false must deny, not grant: Expected: true   Actual: <false> "locations.create": false must deny, not grant
- Note:     F1 reproduced: "locations.create": false did not deny the insert. Any group in production that spells a permission out as false is granting it.

```
#0      checkedTest.<anonymous closure> (file:///Users/mattheus/Development/Projects/ServiceProviders/test/integration/core/checked_case.dart:110:9)
<asynchronous suspension>
#1      Declarer.test.<anonymous closure>.<anonymous closure> (package:test_api/src/backend/declarer.dart:253:15)
<asynchronous suspension>
#2      Declarer.test.<anonymous closure> (package:test_api/src/backend/declarer.dart:250:11)
<asynchronous suspension>
```

### RLS-10 — PermissionGroupModel round-trip must preserve the admin wildcard
- Feature: Work Orders / Permissions & RLS  |  Role: admin  |  Layer: model
- Steps: read every real permission group in this company -> round-trip each one through PermissionGroupModel
- Expected: The wildcard survives. Predicted to FAIL (F4): toJson() rebuilds the JSONB from Map<ResourceType, Set<PermissionAction>>, which has no representation for "*" or for the commented-out resource families.
- Actual:   at least one real group loses permissions through the model
- Error:    TestFailure — a read-modify-write through the model must be lossless: Expected: false   Actual: <true> a read-modify-write through the model must be lossless
- Note:     Group "Supervisor" loses 2 key(s) on a read-modify-write through the model: work_orders.approve_pause, work_orders.approve_completion.
- Note:     Group "Administrador" loses 1 key(s) on a read-modify-write through the model: *.
- Note:     Group "Técnico" loses 3 key(s) on a read-modify-write through the model: reports.read, checklists.read, maintenance_plans.read.
- Note:     F4 reproduced. The dropped keys are those with no ResourceType: "*" plus checklists / reports / maintenance_plans, which are commented out in ResourceType. Saving any affected group through the app silently strips them.

```
#0      checkedTest.<anonymous closure> (file:///Users/mattheus/Development/Projects/ServiceProviders/test/integration/core/checked_case.dart:110:9)
<asynchronous suspension>
#1      Declarer.test.<anonymous closure>.<anonymous closure> (package:test_api/src/backend/declarer.dart:253:15)
<asynchronous suspension>
#2      Declarer.test.<anonymous closure> (package:test_api/src/backend/declarer.dart:250:11)
<asynchronous suspension>
```

